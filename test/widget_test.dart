import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App renders without error', (WidgetTester tester) async {
    // Placeholder test — full widget tests will be added as features stabilize.
    expect(true, isTrue);
  });
}
